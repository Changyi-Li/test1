## 自动分配
在此程序中，你可以创建已现有的项目的清单。通过使用自动分配，你可以将金额例如到不同的成本中心和成本单元。这些自动分配将连接系统已记录的事务进行。你可以定义进行分配所需的条款并确定它们应结果哪些过账。
在系统过账连接，你可以暂时停用特定过账行（例如，登记凭证的自动过账功能。取消复选框 自动过账活动 在过账行中 输入 金额之前在过行中输入金额.复选框 自动过账活动 默认放置在按钮下 更多信息 在行上 .
你可以使用工具栏上的按钮来测试自动分配。也可以使用按钮待复制自动分配 另存为...。
