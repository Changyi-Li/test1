### 列表组件的未来交付可靠性 - 汇总
此列表类型显示为组件未来内部交付可靠性的详细信息，当新建的 新完成日期 日期已通过净需求计算已计算。你可以按照工作中心或实际的（报告）日期对列表组。该列表还可以以图表的形式已呈现在窗口组件。

#### 已计算列：
总数 =准时 (计数) +延迟 (计数)
准时 (%) =（准时 (计数) /总数）x 100
延迟 (%) =（延迟 (计数) /总数）x 100
