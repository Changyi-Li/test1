### 出勤设置列表
你可以在此列表中查询有关员工出勤设置的信息。该列表按员工号排序。
该列表显示名称，参数组，日程表周期，周期有效的日期以及每个员工的主要审核人者。
如果你使列表可以更新（Ctrl + U），然后你可以更改每个员工的参数组。你登记在 参数组 程序。
如果已已激活站点记录（通过系统设置调用 激活站点记录），你以为每个员工选择一个默认打卡站点和一个默认的下班休息站点。你在登记站点 基本数据表 - 出勤 程序。
