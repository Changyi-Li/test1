### Rate type

#### Default rate type
This system setting determines the default rate type on posting rows in another currency than the company currency. If the posting is made using another currency, then the suggested exchange rate will be loaded via the voucher date (the rate that applied to the current currency on that specific date).
