### Settings

#### Pre-select "Save"
Use this setting to decide if the Save checkbox should be marked by default in the list.

#### Only show order rows with price differences
With this setting you determine if only order rows with price differences should be displayed in the list.

#### Only show order rows with expired price
With this setting you decide if only order rows where the price has expired should be displayed in the list.
