### 导入概览列表类型
此列表在具有 EIM 和EIM 工作流选项的系统中可用。它期间有关在已选择的日/例如已导入的发票的信息。如果你工作供应商发票的自动导入，则此列表中的信息可用于更好地概览已自动进入系统的发票。从PDF 收件箱已手动登记的发票不显示在列表中。
例如，你将看到有关已已导入哪种类型的发票（电子发票、 Monitor - to - Monitor、来自 CrossState API）以及导入到哪个收件箱（如果可用此类信息）的信息。发票格式以及发票的当前状态也显示在列表中。
