### 合计列表
此列表类型显示有关内部交付可靠性的合计信息。你可以按工作中心或实际的（报告）日期合计列表。该列表还可以以图表的形式已呈现在窗口组件。

#### 已计算列：
总数量 =准时 (计数) +延迟 (计数) +提早 (计数)
提早 (%) =（提早 (计数) /总数）x 100
准时 (%) =（准时 (计数) /总数）x 100
延迟 (%) =（延迟 (计数) /总数）x 100
