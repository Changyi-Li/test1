### List type Undo split invoice basis
In this list it is possible to undo the split of an invoice basis. To undo a split, both the invoice bases need to be listed, and the split invoice bases need to have status 3 – Pending.
You undo the split by checking the Undo checkbox and then saving.
The system setting Enable split invoice basis needs to be activated in order for the list to be available.
