### 详细 - 案件列表
此列表类型按详细视图中的按案件分组。每个案件都显示在列表中的单独行上。一些列已经合并。这些列显示客户号或供应商号，以及客户名称或供应商名称。由于一个案件不能两者填写客户和供应商的信息，列采用了这种设计。这也适用于案件号和联系人。

#### 拒收数量
在此列，你可以看到该案件拒收数量。

#### 已计划和实际成本
在这些列，你可以看到案件全部成本的合计。这可以每基本类型显示，例如，全部客户偏差都总计起来。这也可以显示为每客户的合计。你将在列表底部看到整个列表的合计。
