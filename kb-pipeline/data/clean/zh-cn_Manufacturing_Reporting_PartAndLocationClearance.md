### 预留组件和库位
此表格根据线边库、可追溯性和已链接订单/需求直连来指示是否会有库位预留或组件预留。
库位预留意味着预留特定库位清算余额。组件预留意味着系统清算该组件的余额，不管该组件余额是从哪个库位已预留的。
- 对于没有线边库的为组件- 无论可追溯性如何 -总是应用库位预留。
- 为组件具有线边库但没有可追溯性的零件 — —总是应用组件预留。
- 对于可以序列号的可追溯性的为组件，总是应用库位预留的方式。
- 为组件具有线边库并且具有批次层级可追溯性的零件 —总是应用组件预留。
在里面 预留 组件列表程序中的列表类型，你可以看到组件的预留和库位的预留。
