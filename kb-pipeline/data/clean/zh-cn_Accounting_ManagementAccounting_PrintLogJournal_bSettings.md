### 设置

#### 手动输入凭证日期
如果已为日记账已配置了与会计的集成，然后可以激活此设置凭证日期在 凭证日期 字段。仅当记录属于已关闭期间且必须在不同期间已记录时才应使用此设置。此设置不可用用于重印的列表类型。

#### 集成
在这里你看到 是的 或 否，让你了解日记账是否已已配置与会计的集成。为每个单独的日记账集成已配置 凭证号序列 / 日记账 程序。

#### 日记账信息
在此表格中，你可以看到有关每个日志的过过账项目编号和无效过账（如果有）的信息。
