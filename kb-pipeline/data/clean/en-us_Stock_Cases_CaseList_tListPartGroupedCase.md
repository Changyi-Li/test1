### The Part grouped by case list type
This list type is grouped by case in a detailed view. Each case is shown on a separate row in the list and shows which parts are included in each case. The list also displays which part is default in the case as well as the parts' serial numbers/batch numbers.
