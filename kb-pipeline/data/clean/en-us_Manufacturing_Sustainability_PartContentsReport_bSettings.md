### Settings

#### Calculate underlying levels
Determines if underlying levels in the structure should be calculated (applies to manufactured parts).

#### Pre-select "Include"
This setting determines whether or not the “Include” box should be checked by default for all rows in the list/result.
