### Settings – Reset annual volume

#### Pre-select "Include"
This setting determines whether or not the “Include” box should be checked by default for all rows in the list/result.

#### Show parts with zero volume
With this setting you determine if only parts with zero in annual volume should be shown in the list.
