## 计算重量
此程序用于时间计算多个结构组件的净值。默认下，列表被用于生产件和虚拟件，但你也可以包括采购件。
列表中结构组件的净重已计算为其包含（并入、基础）组件的净值合计。这些是BOM 与工艺路线程序的物料行列表被用于的组件。
你还可以选择重新计算基础级别组件的净值，之前其净值已添加在一起得出结构组件的合计净重。可以为组件已配置固定净重。对于这些组件，净重将不已重新计算。
你还可以输入不同的条款。这样，你将仅在计算中包括在物料行上已选择了这些条款的基础组件。
物料行上的工具将不被用于在净重计算中（适用于 工具与维护 选项已安装）。
