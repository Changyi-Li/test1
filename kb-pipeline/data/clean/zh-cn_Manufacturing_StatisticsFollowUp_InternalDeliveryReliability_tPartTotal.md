### 列表类型组件 - 合计
这种列表类型显示有关内部交付可靠性的合计信息，按组件分组。然后，计算将仅基于零件最后/最终工序的交付可靠性。该列表还可以以图表的形式已呈现在窗口组件。

#### 已计算列：
总数量 =准时 (计数) +延迟 (计数) +提早 (计数)
提早 (%) =（提早 (计数) /总数）x 100
准时 (%) =（准时 (计数) /总数）x 100
延迟 (%) =（延迟 (计数) /总数）x 100
