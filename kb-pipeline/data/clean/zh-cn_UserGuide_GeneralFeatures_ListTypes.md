### 列表类型
在 Monitor ERP 包含已完成的列表类型，以用于列表程序中的不同目的。
对于每个列表类型，还有一组来自不同登记的标准选择行，并且在许多案件下，还有一组标准的格式。对于许多列表类型，也有不同的设置。
在列表程序中，你开始选择列表类型和格式（如果有）。然后，载入你选择记录，并且可能你在你数据记录列表之前变更设置。
你自己的默认保存​​ [默认值](DefaultValues.htm) 该 功能 可以 在程序工具栏 上 找到 .
