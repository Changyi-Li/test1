### 页眉行

#### 序列号 / 批次号
在此字段中，你选择/输入在此程序中你上班的序列号 / 批次号。

#### 类型
通过此字段，你可以确定输入的编号是序列号还是批次号。当创建自报告中创建此编号时，会自动设置正确的类型。然后，类型取决于填写的是序列号字段还是批次号字段。如果你人工的创建编号，你可以自己选择类型。你不能更改类型已保存记录的类型。

#### 组件
在这里你可以看到一个现有组件编号，该编号与序列号/批号链接至，或者应该与序列号 / 批次号链接至。名称，组件类型和库位会自动从组件登记中加载。
