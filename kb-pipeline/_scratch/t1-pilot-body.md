## Question

把 Getting Started 部分（EN + ZH）端到端跑一遍抽取试点，产出一份可评审的样本数据集。

前置：帮助站点完整清单与中英 URL 映射调查、页面元数据与变更检测信号调查 已闭环。

范围：
- 编写一次性抽取脚本（可复用骨架）：枚举 → 下载 → HTML→Markdown 清洗 → 生成 JSONL 元数据 → 生成一组默认分块。
- 产物放 kb-pipeline/，链接到本票；图片不下载，只在元数据记录 URL。
- 记录抽取问题清单（表格、代码块、中文编码、失效链接等）。
- 附带一个朴素中文检索冒烟测试（5–10 个典型中文问题，关键词或朴素向量检索），供后续质量决策参考。

解决后记录：产物位置、页面数、遇到的问题、冒烟结果。
